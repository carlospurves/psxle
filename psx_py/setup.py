from setuptools import setup

setup(name='psx_py',
    version='0.0.1',
    author='Carlos Purves',
    packages=setuptools.find_packages(),
    description="A Python interface to the PlayStation Learning Environment (PSXLE)."
)
